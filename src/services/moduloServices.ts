import { modulos } from '../interfaces/modulos.interface'
// import { SeguridadDB } from '../config/dbConfig'

const getAllModulos = async (): Promise<modulos | any> => {
  return ''
}

const getModulo = async (_id: string): Promise<modulos | any> => {
  return ''
}

const updateModulo = async (_modulo: modulos): Promise<Number | any> => {
  return ''
}

const insertModulo = async (_datos: modulos): Promise<Number | any> => {
  return ''
}

export default { getAllModulos, getModulo, updateModulo, insertModulo }
